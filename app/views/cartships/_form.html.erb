<%= form_for(cartship) do |f| %>
  <% if cartship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cartship.errors.count, "error") %> prohibited this cartship from being saved:</h2>

      <ul>
      <% cartship.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cart_id %>
    <%= f.number_field :cart_id %>
  </div>

  <div class="field">
    <%= f.label :product_id %>
    <%= f.number_field :product_id %>
  </div>

  <div class="field">
    <%= f.label :quantity %>
    <%= f.number_field :quantity %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
